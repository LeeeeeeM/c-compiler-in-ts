// 测试局部数组
int main() {
    int arr[3];
    int i;
    
    // 初始化数组
    arr[0] = 100;
    arr[1] = 200;
    arr[2] = 300;
    
    // 通过循环访问数组
    i = 0;
    while (i < 3) {
        printf("arr[%d] = %d\n", i, arr[i]);
        i = i + 1;
    }
    
    return 0;
}
